version: '3.9'

services:
  backend:
    build:
      context: ./backend
    ports:
      - '5000:5000'
    env_file:
      - ./backend/.env
    environment:
      REDIS_URL: redis://redis:6379
    depends_on:
      - redis
    networks:
      - app-network

  frontend:
    build:
      context: ./expense
      args:
        REACT_APP_API_BASE_URL: http://localhost:5000/api/v1
    ports:
      - '80:80'
    environment:
      REACT_APP_API_BASE_URL: http://localhost:5000/api/v1
    depends_on:
      - backend
    networks:
      - app-network

  redis:
    image: redis:alpine
    expose:
      - '6379'
    networks:
      - app-network

networks:
  app-network:
    driver: bridge