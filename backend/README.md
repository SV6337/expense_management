# Expense Tracker Backend

Simple Express + Mongoose API for the Expense Tracker app.

Environment variables (create a `.env` file):

- `MONGO_URI` - MongoDB connection string (defaults to `mongodb://127.0.0.1:27017/expense-tracker`)
- `PORT` - server port (defaults to 5000)

Run:

```
cd backend
npm install
npm run dev
```
